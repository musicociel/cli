{
  "name": "@musicociel/cli",
  "description": "Command line utility, related to the Musicociel application, allowing to manage songs (e.g. converting them between multiple formats, synchronizing them with a CouchDB-compatible database)",
  "version": "0.0.0-placeholder",
  "author": "DivDE <divde@musicociel.fr>",
  "license": "AGPL-3.0",
  "scripts": {
    "semantic-release": "semantic-release",
    "watch": "tsc -p tsconfig.json --watch",
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "test": "true"
  },
  "bin": {
    "musicociel": "bin/musicociel"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/musicociel/cli.git"
  },
  "files": [
    "src",
    "build",
    "bin"
  ],
  "devDependencies": {
    "semantic-release": "^6.3.6",
    "typescript": "^2.3.3"
  },
  "dependencies": {
    "@musicociel/song-formats": "^0.1.0",
    "@types/node": "^7.0.22",
    "globby": "^6.1.0",
    "minimist": "^1.2.0",
    "pify": "^3.0.0",
    "pouchdb-adapter-http": "^6.2.0",
    "pouchdb-core": "^6.2.0"
  }
}
